# Building the process

This section will guide you through the process of building the process. The process is built using the `build` script in the `process` directory. The `build` script is a simple shell script that uses the `docker` command to build the process. The `build` script takes the following arguments:
